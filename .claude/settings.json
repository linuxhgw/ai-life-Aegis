{
  "hooks": {
    "Stop": [{
      "matcher": "",
      "hooks": [{
        "type": "command",
        "command": "git status --porcelain && git diff --stat",
        "timeout": 30,
        "statusMessage": "检查 git 变更"
      }]
    }]
  }
}
