{
  "hooks": {
    "Stop": [{
      "matcher": "",
      "hooks": [{
        "type": "command",
        "command": "git status --porcelain | grep -q . && echo \"存在未提交变更，待手动提交\" || echo \"无未提交变更\"",
        "timeout": 30,
        "statusMessage": "检查 git 变更"
      }]
    }]
  }
}
